{
  local_certs
  auto_https off
}

https://localhost:443 {
  tls internal

  # Proxy API requests to backend
  handle /api/* {
    reverse_proxy backend:4000
  }

  # Serve frontend
  handle {
    reverse_proxy frontend:80
  }
}
